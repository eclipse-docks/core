export * from './contribution-targets';

export const COMMAND_SAVE = "command-save"

export const HIDE_DOT_RESOURCE = false

export enum MouseButton {
    LEFT = 0,
    MIDDLE = 1,
    RIGHT = 2,
    BACK = 3,
    FORWARD = 4
}
